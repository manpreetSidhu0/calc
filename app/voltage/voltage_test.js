'use strict';

describe('myApp.voltage module', function() {

  beforeEach(module('myApp.voltage'));

  describe('voltage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('voltageCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});